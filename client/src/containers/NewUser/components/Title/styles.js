import styled from 'styled-components';

import { THEME } from '../../../../constants';

const { GRAY } = THEME;

export const TitleStyled = styled.h2`
  color: ${GRAY};
  text-align: center;
  width: 100%;
`;

export default {
  TitleStyled,
};
